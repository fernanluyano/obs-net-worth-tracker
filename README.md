# 💰 Net Worth Tracker

An [Obsidian](https://obsidian.md) plugin to track net worth, accounts, and budget over time, right inside your vault.

**Status:** early scaffold — no real features yet, just the plugin skeleton (ribbon icon + a "Say hello" command) to build on.

## 📥 Installation

**Manual (development):**

1. Clone this repo into `<your vault>/.obsidian/plugins/net-worth-tracker/`
2. `make install && make build`
3. Reload Obsidian and enable **Net Worth Tracker** under Settings → Community plugins

## 🛠️ Development

See `CLAUDE.md` for project conventions and the `Makefile` for common commands (`make dev`, `make test`, `make build`).

## 📄 License

MIT
